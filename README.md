# RefMind3D

## v1.16.1 高清图片层叠修复

- 修复放大画布后始终只能看到模糊缩略图的问题：缩略图、高清图和分块图现在在同一位置渐进替换。
- 高清缓存解码完成后立即显示，不再被节点裁剪区域隐藏。
- 保留按屏幕像素选择 512 / 1200 / 2400 / 原图的渐进加载策略。

## v1.16.0 屏幕像素驱动的图片流送

- 删除依赖画布缩放、选中状态和资源预算的旧图片清晰度状态机。
- 磁盘缓存作为原图派生的 mip 层，仅用于快速显示和降低解码内存；原图始终保留为最高质量来源。
- 普通节点与 GPU 合成层统一按照节点占用的屏幕物理像素选择最小足够分辨率，并预留 35% 清晰度余量。
- 缓存尚未准备完成时，可见的普通图片直接使用原图兜底；缓存完成后无闪烁切换到合适 mip。
- 缩放过程中复用当前纹理，缩放停止后的视图更新立即重新选择 512/1200/2400/原图。

## v1.15.2 低缩放与 GPU 图片清晰度修复

- 低画布缩放不再无条件锁定 512 像素缩略图，改为按照节点实际占用的屏幕物理像素选择清晰度。
- GPU 图片合成层接入 512/1200/2400/原图分级缓存，不再固定使用缩略图。
- GPU 图片进入视口后会主动准备缓存并热切换到合适清晰度，缩放停止后无需重新打开工程。
- 使用 3840×2160 图片、0.32 倍画布缩放和 125% Windows 显示缩放增加回归测试。

## v1.15.1 放大清晰度修复

- 图片分级加载现在按屏幕物理像素选择缓存层，适配 Windows 高 DPI 显示缩放。
- 提前从 1200 像素中等预览升级到 2400 像素高清预览，避免放大后继续拉伸低清缓存。
- 当前选中和视口附近的大图会优先加载原始分辨率或可见高清切片，并保留缩放回退滞回以避免反复切换。
- 增加 1x/2x 显示缩放和高清层升级回归测试。

## v1.15.0 渲染线程、统一输入与安全诊断

- 极端缩放的缩略图总览改由 OffscreenCanvas Worker 合成，主线程专注交互。
- 总览画布作为单一缩略图图集输出，减少大量小图片的纹理与绘制切换。
- 工程打开增加索引、恢复检查和当前画布三个阶段，其他画布继续延迟加载。
- 新增双指缩放和平移，触控事件按动画帧合并，数位笔尾端支持撤销上一笔涂鸦。
- 新增选中对象 90° 旋转、图片翻转，以及坐标、尺寸、角度精确变换。
- 图片检查新增均色、Alpha 覆盖率与亮度直方图，同时保留差值和同步缩放。
- 新增按文件名与文件大小批量重新定位链接资源，匹配成功后自动刷新缓存。
- 自动画质降级增加迟滞恢复，避免帧率边界附近频繁切换画质。
- 崩溃诊断日志跨会话本地保留，并记录上次会话是否可能异常结束。

## v1.14.0 专业整理、检查与稳定性更新

- 画布节点采用独立绘制隔离和内容可见性裁剪，减少无关区域重绘。
- 图片先显示缓存缩略图，清晰层解码完成后无空白替换。
- 工程继续在后台增量保存，并显示准备、写入和校验阶段。
- 新增对象冻结与解冻，冻结后暂停图片升级、视频和 3D 实时资源。
- 智能参考线新增等间距节奏吸附，按住 Alt 可临时关闭吸附。
- 新增瀑布流、按选择顺序排列，并让锁定对象保持原位。
- 图片 A/B 检查新增差值模式、同步缩放和指针百分比坐标。
- 工程资源管理显示总体积、未使用与超大资源，并可安全移除未使用项。
- 性能诊断扩展为 1K、5K、10K 三档稳定基准，并保留最近一次基准数据。

## v1.13.0 大型画布流畅度与专业工作流

- 极端缩放时切换低分辨率总览图层，远景仍完整显示，同时限制纹理与解码开销。
- WebGL 上下文异常会自动回退并重试，避免驱动重置后长期空白。
- 工程保存会复用未变化画布的压缩数据，只重写发生变化的画布。
- 最近使用的图片会在空闲时低优先级预热，重新打开工程和切回画布更快。
- 鼠标中键拖动画布采用独立高频采样和惯性帧，松开后稳定、连续且不跳转。
- 新增仅整理重叠对象、锁定对象保持原位和重复上次布局。
- 新增两张图片的滑动、闪烁和透明棋盘格 A/B 对比。
- 新增 2048 像素分块的超大画布流式导出，降低峰值内存。
- 新增统一发布稳定门禁，自动检查前端、Rust、回归测试、版本一致性和发布卫生。

## v1.12.1 远景图片显示修复

- 修复画布缩小时，进入视野的图片数量超过 GPU 纹理预算后部分图片消失的问题。
- GPU 合成候选严格限制在纹理预算内，超出预算的图片继续使用缓存缩略图显示。
- 只有纹理真正上传完成后才隐藏普通图片预览，加载失败或仍在等待时不再出现空白节点。
- 视口移动后会清理过期 GPU 纹理，并忽略已经离开视口的异步解码结果。
- 新增 500 张图片远景缩放回归测试，确保所有图片始终存在可见渲染路径。

## v1.12.0 稳定性、完整复制与交付安全

- 工程包在替换原文件前校验索引、画布、资源和 ZIP CRC，损坏的临时保存不会覆盖最后一个正常版本。
- JS 堆内存达到硬阈值后自动清理图片内存缓存、暂停高成本原图与模型，并收紧 GPU 纹理池；压力解除后平滑恢复。
- 新增后台任务中心，保存与校验不再静默执行，失败信息会保留供用户查看。
- WebGL 图层按当前屏幕 DPR 渲染，并监听窗口与 Visual Viewport 变化，改善多显示器和高缩放环境。
- 节点复制会携带选区内部连线、组层级和引用资源，可完整粘贴到其他画布，落点仍以鼠标为中心。
- 对齐与统一尺寸改用最后选中的对象作为锚点，便于可预测的专业排版。
- 诊断报告加入显示缩放、内存保护、工程摘要、完整性结果和脱敏错误日志，不导出 API Key 或本机路径。
- 新增 GitHub 正式发布检查和安装包 SHA-256；发布脚本预留真实 Windows 代码签名证书钩子，未提供证书时不伪造签名。
- 固定基准加入 80 轮索引查询与序列化稳定性循环，覆盖长时间编辑中常见的保存/查询负载。

## v1.11.0 秒开、原生监听与安全恢复

- 工程打开继续采用索引优先和当前画布优先策略，并在性能诊断中记录索引与当前画布的实际打开耗时。
- 超大图片只挂载视口相交瓦片及周围一圈预取瓦片，离开视野的瓦片不进入 DOM 解码队列。
- WebGL 合成层新增裁切 UV、取景、旋转、镜像、灰度和透明度处理；不支持时继续回退普通图片节点。
- 工程监控文件夹改用操作系统原生递归文件事件，文件变化后再增量扫描，不再固定轮询磁盘。
- 未保存编辑每 3 秒生成轻量恢复快照，并额外保留最近 20 个崩溃安全编辑日志。
- 历史版本显示画布、节点和资源数量，支持恢复选中节点、当前画布或整个工程。
- 搜索索引按工程缓存 ID 持久保存，重新打开搜索时可先使用已有索引，再在空闲时间刷新。
- 图片缓存 PNG 继续携带来源 ICC Profile，由 WebView 按当前显示空间进行色彩管理。
- 固定压力基准加入 1,000 张 8K 图片的 120 帧瓦片调度模拟，并记录压力内存估算。

## v1.10.0 专业性能与资源治理

- WebGL 图片层补齐旋转、镜像、灰度与透明度，并加入 256 项纹理 LRU 上限，异步解码仍保持正确层级。
- 图片继续使用缩略、中等、预览和原始分辨率多级缓存，按显示尺寸升降清晰度，视野外释放高成本资源。
- 性能诊断新增 GPU 纹理数量与 P95 输入延迟，便于定位高轮询率鼠标、触控板和数位笔卡顿。
- 工程监控文件夹支持自动导入、仅提醒和暂停，记录新增及失联素材，删除源文件不会删除画布节点。
- 新增视觉相似图片扫描，以 8×8 感知哈希列出候选组，只供人工检查，不自动删除。
- 新增已加标签、最近导入、超大资源、失联资源和全部图片智能集合。
- 工程时间线既可恢复整个工程，也可只恢复当前画布。
- 新导入图片记录嵌入 ICC 状态，结合自动、sRGB 校样和 Display-P3 优先策略管理显示。
- GPU、资源同步、查重和历史功能均保留兼容回退路径，不改变已有工程文件的可打开性。

## v1.9.0 专业参考工作流

- 新增置顶、全屏、参考窗口和临时鼠标穿透模式，参考模式可隐藏编辑界面并保留导航能力。
- 新增画布导航缩略图与命名视图，可快速返回常用区域；各画布的视图独立保存。
- 属性栏补全角度快捷旋转、宽高比锁定、精确尺寸和多选等间距排列。
- 新增全工程搜索与节点/资源标签，按 `Ctrl+Shift+P` 可跨画布检索标题、正文、文件名、路径和标签。
- 图片缓存生成会读取 EXIF 方向；提供自动、sRGB 校样和 Display-P3 优先三种显示策略。
- 每个工程可添加独立监控文件夹，新出现的支持文件会自动导入当前画布。
- 可安全合并内容哈希一致或同源的精确重复资源，并自动迁移节点引用。
- 每次覆盖保存前自动保留旧工程包，跨重启保留最近 12 个版本，可在设置中恢复。
- 大型低缩放画布启用 WebGL 图片合成层，并保留 DOM 回退路径，降低海量图片节点的布局与绘制压力。

## v1.8.0 大型画布流畅度与稳定性

- 画布按背景、连接线、节点与涂鸦隔离绘制，低帧率时自动切换均衡或流畅优先级别。
- 图片按缩略图、中等图、预览图和原图渐进切换，解码完成后再替换画面，减少闪烁。
- 图片任务会根据设备内存和实时帧率自动调整并发，并沿画布移动方向提前加载。
- 超远坐标使用动态渲染原点，降低浮点误差造成的抖动、漂移和缩放跳跃。
- 鼠标、数位笔、裁切、涂鸦、框选、拖动和缩放使用互斥交互状态，避免状态串扰。
- 拖动节点时提供边缘与中心智能参考线；按住 Alt 可临时关闭吸附。
- 可见的链接素材每 5 秒检查大小和修改时间，变化后只刷新对应素材缓存，也可单独关闭自动刷新。
- 手动保存、定时保存和关闭前保存共用串行队列，防止旧快照覆盖新编辑；工程继续使用分画布数据与原子替换。
- 性能诊断新增帧时间、清晰度层级、纹理估算、缓存队列和源文件刷新统计，并可导出 JSON 报告。

## v1.7.0 深度参考工作流

- 双击图片进入画布内裁切编辑：拖动取景、滚轮缩放、八方向调整裁切框，Esc 完成。
- 链接资源自动检查失联状态，支持单项重新定位和批量按文件名匹配。
- 层级面板支持拖拽排序、拖入组建立父子关系、F2 重命名和资源状态标记。
- 排列工具新增紧凑智能装箱、按路径/添加时间/层级顺序排列、随机排列、堆叠及可调间距。
- 新增系统屏幕取色、颜色复制和图片原图坐标检查。
- 最近工程数量、启动页显示和自动打开最近工程均可配置。
- 演示模式支持可调间隔、层级顺序、随机不重复与完全随机，手动翻页自动暂停。
- 已保存工程支持定时覆盖保存；恢复中心记录自动恢复快照状态。
- 新增深色、浅色和玻璃主题，设置支持导入导出，快捷键冲突会立即标记。

## v1.6.0 参考工作流增强

- 图片支持非破坏式裁切画框、画面缩放/取景、水平/垂直镜像、透明度与灰度，原始文件不会被改写。
- 节点层级面板支持搜索、快速定位、显隐、锁定和逐层调整；画布支持整体锁定与灰度检查。
- 新增连续浏览/自动演示模式，可用方向键或 PageUp/PageDown 切换，Esc 退出。
- 新增导出中心，统一整张画布、选区 PNG、原始资源与便携工程副本的出口。
- 资源可逐个选择“嵌入工程”或“链接原文件”，在便携性与工程体积之间自由取舍。
- 空白启动页显示最近工程；排列工具新增按名称智能排列和统一视觉面积。
- PNG 导出与画布显示使用相同的显隐、透明度、灰度、镜像和裁切取景规则。

## v1.5.0 极端工程与交互优化

- v3 工程首次打开只读取工程索引和当前画布，其他画布切换时再从独立数据块加载。
- 平移、缩放和涂鸦继续使用合成层预览、合并指针事件与逐帧提交，减少交互状态更新。
- 根据设备内存、实时帧率和节点数量动态调节高清图片、视频与 3D 模型预算。
- 工程保存移入后台阻塞任务，画布界面不会等待压缩和磁盘写入。
- 图片缓存目录改用内容指纹，同一工程内重复来源直接复用解码结果。
- 图片根据显示尺寸自动升降预览等级，离开视野后释放实时模型和高清资源。
- 以单个画布定时器合并原始素材变化检查，避免大量文件监控事件形成刷新风暴。
- 每次保存保留最近 3 份轻量工程索引；工程包损坏时尝试读取最近索引备份。
- 性能诊断补充视口查询和 120 帧合成压力测试，并保留万人节点基准。

## v1.4.0 大型工程优化

- 非活动画布保持轻量序列化状态，切换时才恢复可编辑对象，并记忆每个画布最后视口。
- 撤销与重做改用按字段补丁，常见移动、缩放和文字编辑只记录节点字段集合。
- 空间索引增量同步，视口保留短时 DOM 节点池，快速往返平移减少反复挂载。
- 3D 预览离开视野后完整释放材质、纹理、渲染列表和 WebGL 上下文。
- 根据上次视口与移动方向进行附近节点预热，图片继续使用分级解码和并发预算。
- 自动恢复只写入上次手动保存后变化过的画布，未变化画布不进入恢复日志。
- 新建工程包采用 v3 分块结构，每张画布拥有独立 JSON 数据块；旧版工程继续兼容。
- 设置中的性能面板新增 10,000 节点基准测试，便于识别设备性能和版本回退。

## v1.3.0 性能与可靠性更新

- 非活动画布以序列化快照驻留，仅在切换时恢复对象，降低多画布工程的常驻内存。
- 撤销栈使用结构共享并按一次交互记录，避免拖动和输入过程中反复复制完整工程。
- 图片解码在 Rust 后台线程执行；视口资源按优先级调度，并限制并发解码数量。
- 已完成的涂鸦通过 OffscreenCanvas 工作线程绘制，当前笔画保持独立低延迟图层。
- 重复导入同一来源资源时复用已有资产记录，减少工程包体积。
- 3D 模型封面统一保存到当前工程缓存目录，可随工程缓存一起管理和清理。
- 更改缓存目录后后台迁移已有缓存，不阻塞画布继续操作。
- 节点数量过大时自动启用简化显示，关闭非必要阴影、标题和非选中模型实时渲染。
- 设置中新增工程健康检查，可发现失效资源引用、断裂连线、重复资源、空分组和超大文件。

RefMind3D 是面向设计师和 3D 创作者的无限参考画布，用于统一整理图片、视频、文档、表格、3D 模型、思维导图与创意资料。

## 主要能力

- 多画布工程以及图片、文本、视频、文档、表格和 3D 模型节点。
- 文本与图片节点支持跨画布复制、粘贴，并以鼠标位置为节点中心。
- 支持自由画笔、压感、箭头、空心矩形和空心圆形涂鸦；涂鸦保存在工程中，但不随图片导出。
- 每个工程拥有独立图片缓存目录，缓存可跨版本保留，默认容量上限为 10 GB。
- 支持本地 Ollama、OpenAI 兼容服务、豆包和自定义 HTTP AI 服务。

## v1.2.0 性能与可靠性

- 使用空间索引查询视野附近节点，大型画布移动时不再遍历全部节点。
- 画布平移与节点拖动采用合成层预览，操作结束后才提交工程状态。
- 涂鸦改由双 Canvas 图层绘制，已完成笔迹与当前笔迹分层渲染。
- 图片按照缩略图、缓存预览、原始分辨率三级渐进加载。
- 撤销历史采用结构共享，避免每次修改都完整复制大型工程。
- 工程脏状态使用轻量修订签名，不再在每次界面更新时序列化整个工程。
- 3D 引擎按需加载，明显缩小首屏 JavaScript 体积。
- 工程采用临时文件完整写入后原子替换，并为已保存工程生成不重复打包原始素材的轻量恢复快照。
- 工程指定的缓存目录暂时不可用时，图片加载会回退到系统缓存目录。

## v1.2.1 持续流畅优化

- 节点缩放期间使用 DOM 合成预览，松开鼠标后一次提交尺寸和字号。
- 图片缓存生成使用三级优先级队列，限制同时解码数量并取消失去全部消费者的排队任务。
- 前端图片缓存采用 LRU 淘汰；窗口隐藏时主动降级超高清图片。
- 涂鸦只重绘当前视野相交的笔迹，连线通过节点邻接索引获取。
- 自动恢复改为轻量 JSON 快照，恢复时复用正式工程资源，不再重复打包未变化素材。
- 清理同一素材失效的运行时缓存映射，控制长时间运行的内存增长。

## 开发

首次运行开发版可以双击 `run-dev.bat`，也可以执行：

```powershell
npm install
npm run tauri:dev
```

检查、测试和构建前端：

```powershell
npm run check
npm test
npm run build
```

运行 Rust 测试：

```powershell
cargo test --manifest-path src-tauri/Cargo.toml
```

生成 Windows 发布包可以双击 `build-release.bat`。

## 图片缓存

- 默认目录位于 `%LOCALAPPDATA%\RefMind3D\ImageCache\<工程缓存 ID>`。
- 用户可以在“设置 → 图片缓存”中为每个工程选择不同目录。
- 工程中的所有画布共用该工程的缓存目录。
- 原始文件大小或修改时间发生变化时，缓存自动失效并重新生成。
- 可以清理当前工程 30 天前的缓存或全部缓存；清理缓存不会删除工程和原始素材。
- 自定义目录不可用时，设置界面会提示重新选择，但图片会临时使用系统缓存继续加载。

## AI 接口与使用规则

“内容分析”和“图片生成”使用两套独立配置。内容分析用于文字回复、内容整理和图片理解；图片生成必须连接真正支持图像生成的服务。

1. 在画布中右键打开“设置 → AI 接口”。
2. 分别填写服务类型、API 地址、API Key 和模型名称。
3. 选择画布节点后使用“发送分析”，或使用“生成图片到画布”。
4. 单次视觉分析最多附带 4 张选中图片，以避免请求体过大。

使用第三方服务时，请遵守服务商的内容政策、授权条款、计费规则和速率限制。发送分析时，选中节点的文字、元信息和图片可能上传到所配置的服务；处理敏感信息前请确认服务商的数据与隐私政策。AI 输出仅作为创作辅助，发布或商用前应人工核对准确性、版权和可用性。

## 构建文档

- [功能说明](docs/features.md)
- [测试清单](docs/testing.md)
- [开发路线](docs/roadmap.md)
- [构建说明](docs/build.md)
